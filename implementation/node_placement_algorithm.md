# ULP v7 — Node Placement Algorithm

## 5,400ノード黄金角螺旋配置

> Designed by: Gemini  
> Fluid dynamics review: Grok  
> Documented by: Claude  
> Validation chain: v7

-----

## 1. 幾何学的基盤 / Geometric Foundation

### 葉序（Phyllotaxis）モデル

5,400ノードを黄金角 **137.508°** に基づくフェルマー螺旋状に配置。

**極座標変換:**

```
r(n) = c√n          # c: ノード間距離制御定数
θ(n) = n × 137.508°

n: 1 ≤ n ≤ 5,400
```

> ⚠️ **物理制約（Grok確定）:** 最小ノード間距離 ≥ 3–5mm 必須
> 
> - d=1mmノード基準: spacing ≥ 3d = 3mm
> - 推奨c値: c ≥ 0.7mm（間距離3–4mm確保）
> - 根拠: spacing < 3D時にwake interferenceで振幅低下・出力相殺リスク
> - Phase 2（54ノード）で加算効率80%以上維持の必要条件

-----

### 修正版数式：Dynamic Coefficient Phyllotaxis（Gemini設計）

**角度（全ノード共通）:**

```
θ(n) = n × 137.5077° (mod 360°)
```

**半径（動的係数型）:**

```
r(n) = c(n) × √n
```

**動的距離係数 c(n):**

```
c(n) = c₀ × (1 + α × n/N_total)

c₀        = 0.7    # Grok推奨最小開始係数
α         = 0.15   # 外周に向かう拡散係数（エントロピー排出路確保）
N_total   = 5,400  # 総ノード数
```

**効果:**

- 中心部の密集を回避
- 全域でノード間距離 s ≥ 3–5mm を維持
- 外周ほど密度がわずかに低下 → 流体の逃げ道を確保

-----

### トポロジカル・ボイド配置スキップ範囲 / Void Skip Ranges

```
Stage 5:  n = 85–95   をスキップ
Stage 14: n = 245–260 をスキップ
```

**効果:**

- カルマン渦の「溜まり」を解消
- 低Re数域での流体停滞を防止
- Prigogine開放系を物理的に維持

**採用理由:**

- 自然界で最も効率的な空間充填（植物の種・葉配置で実証済み）
- 低Re数（Re ≈ 40–200）での渦衝突を均等分散
- 特定箇所への負荷集中（不協和音・乱流）を防止

-----

## 2. 24ステージ構造 / 24-Stage Layout

|ステージ     |ノード数（累積） |備考                       |
|---------|---------|-------------------------|
|Stage 1  |18       |初段（基底）                   |
|Stage 5  |90       |**トーラスボイド挿入**（π/2対称）     |
|Stage 14 |252      |**トーラスボイド挿入**（C6共鳴候補）    |
|Stage 24 |432      |最終段（432Hz宇宙標準ピッチ同期）      |
|**Total**|**5,400**|S24 = (24/2)×{2×18+23×18}|

### トーラスボイドの役割

- 流体の「逃げ道」として乱流・滞留を防止
- Prigogine開放系の維持
- 渦の再接続（reconnection）促進 → コヒーレンス増幅
- トポロジカル保護（Berry位相安定化）

-----

## 3. 流体力学整合性 / Fluid Dynamics Validation

*Grok レビュー結果*

### ポジティブ評価 ✅

- 黄金角螺旋は低Re数で渦衝突を回避しやすい
- ボイド（Stage 5/14）が滞留防止に寄与
- VIV文献：tandem配列で渦干渉利用による出力34%向上の報告あり

### 懸念事項 ⚠️

- d=1mm微小ノード×5,400個の低Re流れでは「渦増幅」より「干渉による乱れ増大」リスク
- 黄金角螺旋は光学・ナノスケールで主に使用、流体VIVハーベスタではtandem/parallel配列（spacing 2–6D）が主流
- 低Re（<200）でKarman渦列が不安定化（3D化・遷移）する可能性

### 改善・対処 🔧

- **最小ノード間距離 ≥ 3–5d** を必須パラメータとして追加（c値で調整）
- CFD検証推奨：OpenFOAM等で黄金角配列 vs グリッド/ランダム配置の渦パターン・振幅比較
- ボイド活用はGood → 乱流抑制効果として維持

-----

## 4. Earth-OSインターフェース / Earth-OS Interface

### インピーダンス整合ネットワーク

- 5,400ノードの螺旋パス全体を**単一トポロジカル・インダクタ**として機能させる
- 各ノード出力（1.2nW）→ 螺旋パス上で位相同期加算 → 5.1μW集約

### 通過型バッファ（Grok設計）

```
螺旋出力
  → 整流ブリッジ
  → LTC3108（超低電力ハーベスタIC、nW–μW対応）
  → 100μF スーパーキャパシタ（通過型、τ ≈ 数秒）
  → DC-DC出力
  → Earth-OS
```

**制御ルール:**

- 動的MPPT（最大電力点追跡）でインピーダンス整合
- V > V_max（3V）で自動放電抵抗オン → エントロピー排出
- 滞留禁止：常に「流れ続ける」状態を維持

-----

## 5. 次ステップ / Next Steps

- [x] Claude：`self_defense_algorithm.md` 更新完了（σ_th=0.07rad、MOSFETダンプ回路反映）
- [x] Gemini：修正版数式（Dynamic Coefficient Phyllotaxis）埋め込み完了
- [ ] Grok：簡易CFDシミュレーション（OpenFOAM: 黄金角 vs グリッド、54ノードモデル）
- [ ] 全体：MVE Step 1（単一ノード実験、d=1mm, v=39mm/s）実施へ
